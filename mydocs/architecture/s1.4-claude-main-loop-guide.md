# s1.4 Claude Code 主循环泳道图 - 执行指南

> **子阶段**：阶段1 子4
> **问题定义**：Claude Code 的主循环是怎样的？（基于代码阅读，不启动）
> **建议时长**：3-5 天（业余学习，每天 1-2 小时）
> **前置**：s1.2 和 s1.3 已完成

---

## 本指南的目的

本指南将帮助你创建 `s1.4-claude-main-loop.svg` 泳道图，建立对 Claude Code 主循环的视觉化理解。

---

## 步骤1：回顾 s1.3 的经验（预计15分钟）

### 1.1 你已经掌握的技能

通过完成 s1.3，你已经掌握了：
1. **泳道图的基本概念**：泳道、活动、流程线
2. **绘图工具的使用**：draw.io 的基本操作
3. **主循环分析方法**：如何识别关键步骤、分配到泳道

### 1.2 应用到 Claude Code

Claude Code 的主循环可能与 Hermes Agent 类似，但会有一些差异：
1. **语言差异**：Claude Code 使用 TypeScript，Hermes Agent 使用 Python
2. **架构差异**：Claude Code 可能使用不同的设计模式
3. **工具系统差异**：工具注册、执行、调度可能不同

### 1.3 调整分析方法

1. **代码阅读**：重点关注 TypeScript 代码，而不是 Python 代码
2. **泳道设计**：可能需要调整泳道数量 and 名称
3. **步骤识别**：识别 Claude Code 特有的步骤

**✅ 完成标准**：
- [ ] 已回顾 s1.3 的经验
- [ ] 已理解需要将 s1.3 的经验应用到 Claude Code

---

## 步骤2：分析 Claude Code 主循环（预计1-2小时）

### 2.1 回顾 s1.2 的代码阅读

在 s1.2 中，你已经对 Claude Code 的架构有了初步认识。回顾你的发现：
1. **工具系统对比**：Claude Code 的工具注册方式、Schema 定义方式
2. **主循环对比**：Claude Code 的主循环位置、迭代控制、预算控制、压缩策略

### 2.2 深入阅读关键代码

**文件1：主循环文件**
- 可能在 `packages/cli/src/agents/loop.ts` 或类似文件
- 重点阅读：主循环函数、循环条件、迭代控制

**文件2：工具执行文件**
- 可能在 `packages/cli/src/tools/` 目录
- 重点阅读：工具执行函数、工具调度

**文件3：API调用文件**
- 可能在 `packages/cli/src/api/` 目录
- 重点阅读：API调用函数、重试机制

### 2.3 识别关键步骤

基于代码阅读，识别 Claude Code 主循环的关键步骤：

1. **初始化**：
   - [ ] 生成 task_id 或类似标识
   - [ ] 重置重试计数器
   - [ ] 设置迭代预算
   - [ ] 加载/构建 system_prompt
   - [ ] 预压缩上下文（如果超出阈值）

2. **添加用户消息**：
   - [ ] 将用户输入添加到 messages[]

3. **循环开始**：
   - [ ] 检查中断请求
   - [ ] 检查预算
   - [ ] 构建 api_messages
   - [ ] 调用 LLM API

4. **处理响应**：
   - [ ] 被截断 → 请求继续/重试
   - [ ] 有 tool_calls → 执行工具 → 继续循环
   - [ ] 纯文本 → 作为 final_response → 退出循环

5. **错误恢复**：
   - [ ] 压缩上下文
   - [ ] 切换 fallback provider
   - [ ] 指数退避重试

6. **收尾**：
   - [ ] 保存 session
   - [ ] 触发 hooks
   - [ ] 返回结果

### 2.4 分配到泳道

将关键步骤分配到对应的泳道：

**泳道1：用户**
- 输入请求
- 接收回复

**泳道2：CLI**
- 显示输入提示
- 显示输出
- 处理中断信号

**泳道3：Agent Controller**
- 初始化
- 添加用户消息
- 循环控制
- 构建 api_messages
- 处理响应
- 错误恢复
- 收尾

**泳道4：LLM API**
- 接收请求
- 生成回复
- 返回 tool_calls 或 content

**泳道5：工具系统**
- 执行工具
- 返回结果

**泳道6：上下文管理**
- 压缩上下文
- 管理对话历史

**提示**：
- Claude Code 的泳道可能与 Hermes Agent 类似，但名称可能不同
- 重点是理解流程，而不是完美分配

**✅ 完成标准**：
- [ ] 已深入阅读 Claude Code 的关键代码
- [ ] 已识别关键步骤（至少10个，可能比 Hermes Agent 少）
- [ ] 已将步骤分配到泳道（至少5个泳道，可能比 Hermes Agent 少）

---

## 步骤3：绘制泳道图（预计1-2小时）

### 3.1 创建泳道

1. 打开 draw.io
2. 选择"Swimlane"工具
3. 创建6个泳道，分别命名为：
   - 用户
   - CLI
   - Agent Controller
   - LLM API
   - 工具系统
   - 上下文管理

**提示**：
- 泳道名称可能与 s1.3 不同，根据 Claude Code 的实际架构调整
- 如果某些泳道不需要，可以删除

### 3.2 添加步骤

1. 在每个泳道中添加对应的步骤（使用"Rectangle"）
2. 使用箭头连接步骤，表示流程方向
3. 添加决策点（菱形）表示条件判断

### 3.3 对比 s1.3 的泳道图

完成初稿后，与 s1.3 的泳道图进行对比：
1. **相同点**：记录相同的部分
2. **不同点**：记录不同的部分
3. **原因**：分析为什么会有这些不同

### 3.4 美化图表

1. 统一颜色方案（可以与 s1.3 不同）
2. 调整字体大小
3. 对齐元素
4. 添加标题和图例

**✅ 完成标准**：
- [ ] 泳道图有对应 Claude Code 的泳道
- [ ] 图表清晰、美观
- [ ] 已与 s1.3 的泳道图进行对比

---

## 步骤4：验证和改进（预计30分钟）

### 4.1 自验证清单

- [ ] 泳道图有对应 Claude Code 的泳道
- [ ] 能口头讲一遍这张图（基于代码理解）

### 4.2 口头讲解练习

尝试向一个假想的朋友讲解这张图：
1. 从用户敲击回车开始
2. 一步步讲解每个泳道的操作
3. 重点讲解与 Hermes Agent 的不同点
4. 讲解你观察到的设计差异

### 4.3 改进图表

根据讲解中发现的问题，改进图表：
1. 添加缺失的步骤
2. 调整流程线
3. 澄清模糊的部分

### 4.4 导出SVG

1. 在 draw.io 中：File → Export as → SVG
2. 保存为 `s1.4-claude-main-loop.svg`
3. 放到 `mydocs/architecture/` 目录下

**✅ 完成标准**：
- [ ] 能口头讲一遍这张图
- [ ] 导出的SVG文件已保存到正确位置

---

## 步骤5：总结对比（预计30分钟）

### 5.1 填写对比分析表

打开 `s1.4-claude-main-loop.svg` 的说明文件（如果需要），或创建 `s1.4-claude-main-loop-notes.md`，填写对比分析：

```
# Claude Code 主循环泳道图 - 对比分析

## 与 Hermes Agent 的相同点

1. [例如：都使用类似的循环结构]
2. [例如：都有工具调用流程]
3. [例如：都有错误处理机制]

## 与 Hermes Agent 的不同点

1. [例如：Claude Code 使用 TypeScript，而 Hermes Agent 使用 Python]
2. [例如：Claude Code 的工具注册更分布式]
3. [例如：Claude Code 更强调类型安全]

## 设计哲学差异

[例如：Claude Code 更强调用户体验，而 Hermes Agent 更强调可扩展性]
```

### 5.2 更新 s1.2 的对比分析

基于你创建的泳道图，更新 `s1.2-claude-first-impression.md` 中的对比分析表：

```
### 2.1 工具系统对比

| 维度 | Hermes Agent | Claude Code |
|------|--------------|-------------|
| 工具注册方式 | `tools/registry.py` 中的 `registry.register()` | [更新为你通过观察泳道图发现的内容] |
| 工具 Schema | JSON Schema | [更新为你通过观察泳道图发现的内容] |
| 权限控制 | `checkPermissions()` | [更新为你通过观察泳道图发现的内容] |
| 并发调度 | `partitionToolCalls` | [更新为你通过观察泳道图发现的内容] |
```

**✅ 完成标准**：
- [ ] 已填写对比分析表
- [ ] 已更新 s1.2 的对比分析

---

## 步骤6：下一步（预计5分钟）

完成 `s1.4` 后，阶段1就全部完成了！

**下一步选项**：
1. **进入阶段2**：开始框架理解 + 单点突破
2. **复习阶段1**：回顾所有产出物，确保理解
3. **优化产出物**：改进泳道图、补充笔记

**提示**：阶段1的产出物是后续阶段的基础，建议确保理解后再进入阶段2。

---

## 附录：Claude Code 主循环特点（供参考）

### A1：可能的特点

1. **类型安全**：TypeScript 的静态类型系统可能使代码更健壮
2. **异步处理**：TypeScript 的 async/await 可能使异步代码更清晰
3. **模块化**：可能使用更细粒度的模块划分
4. **用户体验**：可能更强调用户界面的流畅性

### A2：如何验证这些特点？

1. **阅读代码**：观察类型定义、异步处理、模块划分
2. **对比泳道图**：观察流程设计的差异
3. **阅读文档**：查看 Claude Code 的设计文档或注释

### A3：常见问题

**Q1：找不到主循环文件怎么办？**
- 搜索关键词：`loop`、`agent`、`run`、`conversation`
- 查看目录结构：`packages/cli/src/agents/`

**Q2：代码太复杂看不懂怎么办？**
- 阶段1的目标是建立"粗糙地图"，不是深入理解每个细节
- 重点关注主循环的结构，而不是实现细节
- 记录疑问，后续阶段再深入

**Q3：泳道图与 Hermes Agent 太相似怎么办？**
- 这是正常的，因为两者都是 Agent 系统，设计思路可能类似
- 重点关注差异，而不是相同点
- 如果确实太相似，可以简化 Claude Code 的泳道图

---

**最后更新**：2026-05-03  
**状态**：✅ 待执行 / ⬜ 执行中 / ✓ 已完成
