# s1.2 Claude Code 代码初读

> **子阶段**：阶段 1 子 2
> **问题定义**：Claude Code 的架构设计有哪些值得 Hermes Agent 借鉴的地方？
> **建议时长**：3-5 天（业余学习，每天 1-2 小时）
> **前置**：s1.1 已完成
>
> ⚠️ **重要**：本阶段**只做代码分析，不编译、不启动 Claude Code**。

---

## 1. 代码获取与阅读准备

- [ ] Clone Claude Code 仓库（只读，不安装依赖）
  ```bash
  git clone https://github.com/anthropics/claude-code.git
  cd claude-code
  git checkout <稳定版本 tag>
  ```
- [ ] 用 VS Code / Cursor 打开，只读代码，不 `npm install`
- [ ] 确认 Python 侧无需任何环境（Claude Code 是 TypeScript 项目）

---

## 2. 对比分析

### 2.1 工具系统对比

| 维度 | Hermes Agent | Claude Code |
|------|--------------|-------------|
| 工具注册方式 | `tools/registry.py` | `Tool.ts` |
| 工具 Schema | JSON Schema | Zod Schema |
| 权限控制 | `checkPermissions()` | `checkPermissions()` |
| 并发调度 | `partitionToolCalls` | `partitionToolCalls` |

### 2.2 主循环对比

| 维度 | Hermes Agent | Claude Code |
|------|--------------|-------------|
| 主循环位置 | `run_agent.py` | `query.ts` |
| 迭代控制 | `max_iterations` | `maxTurns` |
| 预算控制 | `iteration_budget` | `checkTokenBudget()` |
| 压缩策略 | `autoCompact` | `autoCompact` |

---

## 3. 3 个意外发现

1. 
2.
3.

---

## 4. 下一步

→ 进入 s1.3（画主循环泳道图）

---

**最后更新**：2026-05-03
**状态**：⬜ In Progress / ✓ Done
